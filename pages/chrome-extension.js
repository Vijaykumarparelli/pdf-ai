import Head from 'next/head';
import Layout from '@components/laylout';
import styles from '@styles/home.module.scss';
import About from '@views/chrome-extension/about';
import UseCase from '@views/chrome-extension/use-case';
import GetStart from '@views/chrome-extension/get-started';
import Banner from '@views/chrome-extension/banner';

export default function Home() {
  return (
    <>
      <Head>
        <title>chrome-extension</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Layout isFooter={true}>
        <Banner styles={styles} />
        <About styles={styles} />
        <UseCase styles={styles} />
        <GetStart styles={styles} />
      </Layout>
    </>
  );
}
