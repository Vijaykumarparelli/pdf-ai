import Link from 'next/link';
import { Button } from 'react-bootstrap';

const ChromeExtensionBtn = ({ styles }) => {
  return (
    <div className="mt-4 d-flex align-items-center">
      <Button variant="ai-primary">Add PDF.ai extension to Chrome</Button>
    </div>
  );
};
export default ChromeExtensionBtn;
